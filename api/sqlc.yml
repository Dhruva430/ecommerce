version: "2"
sql:
  - engine: "postgresql"
    schema:
      - "db/migrations/"
    queries:
      - "db/queries/"
    gen:
      go:
        package: "db"
        out: "models/db"
        emit_pointers_for_null_types: true
        emit_json_tags: true
        emit_interface: true
