version: "2"
sql:
  - engine: "postgresql"
    schema:
      - "db/schema.sql"
    queries:
      - "db/queries.sql"
    gen:
      go:
        package: "db"
        out: "models/db"
        emit_json_tags: true
        emit_interface: true
